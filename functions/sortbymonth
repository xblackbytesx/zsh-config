function sortbymonth {
  echo "Sorting photos by month for year ${PWD##*/}"
  
  for i in {01..12}
    do
      echo "Create dir and move photo's for month $i"
      mkdir ./$i
      mv *${PWD##*/}$i**_* $i/
    done
}

